# requirements-docker.txt — Minimal deps for the OpenEnv environment
# Used by Dockerfile only. Versions pinned for reproducible builds.

fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
openai==1.58.1
requests==2.32.3
